# Repo-Specific Instructions Template

- Results and logs directories contain history and outputs from many runs. Reading all of them will consume a lot of context. Thus an agent should avoid reading such results/logs from old runs unless there is a strong reason to read them. By default, look only at the most recent run if needed.
